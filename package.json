{
  "name": "solrexample",
  "version": "1.0.0",
  "description": "Solr example",
  "main": "index.js",
  "scripts": {
    "build": "browserify index.js -o dist/bundle.js",
    "start": "browserify index.js -o dist/bundle.js & open index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Manuel Ochoa",
  "license": "ISC",
  "dependencies": {
    "jquery": "^3.2.1",
    "solr-client": "^0.7.0"
  }
}
